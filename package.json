{
  "name": "integraciones-os",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencias compartidas del repo (helpers lib/*)",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:py": "pytest tests/python/"
  },
  "dependencies": {
    "mysql2": "^3.11.0",
    "ssh2": "^1.15.0"
  }
}
